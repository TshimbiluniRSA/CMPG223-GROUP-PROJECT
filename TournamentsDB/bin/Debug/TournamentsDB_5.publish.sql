/*
Deployment script for TournamentsDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "TournamentsDB"
:setvar DefaultFilePrefix "TournamentsDB"
:setvar DefaultDataPath "C:\Users\Aaryadev\AppData\Local\Microsoft\VisualStudio\SSDT\"
:setvar DefaultLogPath "C:\Users\Aaryadev\AppData\Local\Microsoft\VisualStudio\SSDT\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[EmployeesInfo].[Username] on table [dbo].[EmployeesInfo] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[EmployeesInfo])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[EmployeesInfo]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_EmployeesInfo] (
    [Id]        INT           IDENTITY (1, 1) NOT NULL,
    [FirstName] NVARCHAR (40) NULL,
    [LastName]  NVARCHAR (40) NULL,
    [Username]  NVARCHAR (40) NOT NULL,
    [Password]  NCHAR (20)    NULL
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[EmployeesInfo])
    BEGIN
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_EmployeesInfo] ON;
        INSERT INTO [dbo].[tmp_ms_xx_EmployeesInfo] ([Id], [FirstName], [LastName], [Password])
        SELECT [Id],
               [FirstName],
               [LastName],
               [Password]
        FROM   [dbo].[EmployeesInfo];
        SET IDENTITY_INSERT [dbo].[tmp_ms_xx_EmployeesInfo] OFF;
    END

DROP TABLE [dbo].[EmployeesInfo];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_EmployeesInfo]', N'EmployeesInfo';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Update complete.';


GO
